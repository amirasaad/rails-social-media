<%= simple_form_for :login, { url: session_path, :html => { :class => 'form-horizontal'}}  do |f| %>
  <%= f.input :username %>
  <%= f.input :password %>
  <%= f.button :submit, "Sign In" %>
<% end %>