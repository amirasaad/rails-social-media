
<%= simple_form_for [post, post.comments.new], :html => { :class => 'form-horizontal' ,:id => 'comment_form' } , remote: true    do |f| %>

	<%= f.input :body  ,label: false,placeholder: 'Write a comment' %>
	<%= f.button :submit , 'Post' ,:class =>'btn btn-primary' %>

	

<% end %>