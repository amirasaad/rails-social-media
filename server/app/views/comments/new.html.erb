<%= simple_form_for[@post, @post.comments.new], remote: true, :html => {:id => 'comment_form' } do |f| %>
	<%= f.input :body  ,label: false,placeholder:'Write a comment' %>
	<%= f.button :submit , 'Comment'  %>
<% end %>