Name <%= current_user.profile.name %>
Birthday <%= current_user.profile.birthday %>

<%= bootstrap_form_for current_user.profile do |f| %>
  <%= f.text_field :name %>
  <%= f.date_field :birthday %>
  <%= f.submit 'Save Changes' ,:class =>'btn btn-inverse' %>
<% end %>

