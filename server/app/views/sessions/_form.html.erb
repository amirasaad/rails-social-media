<%=  bootstrap_form_for :login, { url: session_path}  do |f| %>
	<%= f.text_field :username, :placeholder => 'Your Username', icon: 'user' %>
	<%= f.password_field :password, :placeholder => 'Your password', icon: 'eye-close' %>
	<%= f.submit "Sign In" %>
<% end %>
