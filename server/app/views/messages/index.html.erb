<h1>Chat</h1>

<ul id="chat">
  <%= render @messages %>
</ul>

<%= form_for Message.new, remote: true do |f| %>
<%= csrf_meta_tags %>
  <%= f.text_field :reciver_id %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>